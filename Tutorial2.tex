\documentclass[11pt]{article}
%IF you want a hard return like complelty a new line then just leave a space but if you want a soft return add \\ it will start like a new line ish...

%You can use { } to force the equation to be in the same line as it is written , $ -> represesnts math mode
%If you use $$ -> it is displayed math mode makes it stand out look good 
\begin{document}
Hello! This is my first \LaTeX\ document.

A rectangle has side lengths of $(x+1)$ and $(x+3)$.
The Equation $${A(x) = x^2 + 4x + 3}$$ gives the area of the rectangle.
\end{document}